name: finance_tracker
description: Finance Tracker with Clean Architecture
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  go_router: ^14.0.0
  riverpod: ^2.5.0
  flutter_riverpod: ^2.5.0
  
  firebase_core: ^2.24.0
  cloud_firestore: ^4.14.0
  
  shared_preferences: ^2.2.2
  intl: ^0.18.1
  fl_chart: ^0.66.0
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true