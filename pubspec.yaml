name: finance_tracker
description: Personal Finance Tracker with Clean Architecture
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  go_router: ^14.0.0
  riverpod: ^2.5.0
  flutter_riverpod: ^2.5.0
  
  drift: ^2.14.0
  sqlite3_flutter_libs: ^0.5.0
  path_provider: ^2.1.0
  path: ^1.9.0
  
  chopper: ^7.0.0
  logging: ^1.2.0
  
  json_annotation: ^4.8.1
  equatable: ^2.0.5
  
  shared_preferences: ^2.2.2
  
  firebase_core: ^2.24.0
  cloud_firestore: ^4.14.0
  
  intl: ^0.18.1
  fl_chart: ^0.66.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.8
  drift_dev: ^2.14.0
  chopper_generator: ^7.0.0
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true